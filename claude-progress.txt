# AITaskQueue — Progress Log
# 每次 Claude Code session 结束时更新此文件，下次 session 开始时读取以恢复上下文。

## Current Status: v0.3.0 — 前端 + 后端 + Orchestrator + 基础设施完成

## Completed ✅ (Summary)

- Phase 0-8: 完整前端 UI (Dashboard, Queue/Kanban, Agents, History, TaskDrawer, ChatPanel, NewTaskModal, Notifications)
- Phase 10: 收尾打磨 (键盘快捷键, 过渡动画, 右键上下文菜单, 模板清理, 测试配置)
- Backend-1: FastAPI + PostgreSQL + SQLAlchemy + REST API (tasks, agents, activities, history, chat)
- Backend-2: WebSocket 实时推送 (full_sync + incremental) + 前端 live/mock 双模式
- Backend-3: Claude CLI subprocess engine + PriorityScheduler + OrchestratorEngine
- Infra: Docker Compose 三服务, Dockerfile.frontend + nginx, pnpm dev:all 一键开发
- Dark/Light 主题切换, blocked 默认状态, 项目分组 + 子任务

## Verification
- `pnpm build` ✅ (tsc + vite build, 0 errors)
- `pnpm test` ✅ (43 tests passing)
- `pnpm dev` ✅ (dev server at http://localhost:5173)

## How to Use

### Mock mode (default, no backend needed):
```
VITE_BACKEND_MODE=mock pnpm dev
```

### Live mode (requires PostgreSQL + backend):
```
pnpm dev:all   # one command starts db + backend + frontend
```

Or manually:
```
# Terminal 1: Start PostgreSQL
docker compose up -d db

# Terminal 2: Start backend
cd backend && source .venv/bin/activate && uvicorn app.main:app --reload --port 8000

# Terminal 3: Start frontend
VITE_BACKEND_MODE=live pnpm dev
```

### Full Docker deployment:
```
docker compose up --build
# Open http://localhost:5173
```

## Architecture Notes
- ClaudeCodeRunner uses asyncio.create_subprocess_exec to spawn Claude CLI (not claude_agent_sdk)
- stream-json mode for tasks (real-time progress), json mode for chat (single-turn)
- PriorityScheduler is stateless (pure function: list in → list out)
- Orchestrator.start() resets stale RUNNING tasks to QUEUED on restart
- Three queue types: auto→done, semi→review (wait for approval), human→done
- Frontend 'live' mode driven by WebSocket; 'mock' mode runs MockSimulator
- Task runners execute in parallel asyncio tasks, each consuming a CLI subprocess stream

## Not Started ⏳

### Phase 9: Mock Simulator 端到端验证
- [ ] 打开浏览器端到端验证所有 simulator 行为
- [ ] 验证进度条实时更新、任务自动完成/验收、随机入队

### Backend remaining:
- [ ] Alembic migration 执行验证
- [ ] 端到端测试 (create task → orchestrator picks up → Claude executes → WS updates → UI reflects)
- [ ] Backend unit tests (orchestrator, subprocess_runner, ws_handler)
